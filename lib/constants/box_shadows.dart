import 'package:flutter/material.dart';

import 'colors.dart';

const BoxShadow shadow1 = BoxShadow(
  color: Color(0xffA6BCD0),
  offset: Offset(0, 5),
  blurRadius: 30,
);

const BoxShadow shadow2 = BoxShadow(
  color: gray,
  offset: Offset(0, 6),
  blurRadius: 12,
);